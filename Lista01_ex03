using System;
/*Faça o algoritmo (diagrama de blocos e português estruturado) para calcular o valor 

monetário do desconto (D), sendo que o preço do produto (PR) é fornecido pelo usuário e o desconto é de 5%. 

Apresentar o valor do desconto. */
class Program {
  public static void Main (string[] args) {
    double D ,PR ;
    Console.Write ("digite o valor do produto: ");
    PR =double.Parse(Console.ReadLine());
    D=(5*PR)/100;
    Console.WriteLine ("O desconto será de: R$ {0}", D);
  }
}
